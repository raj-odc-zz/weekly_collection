<%= form_for(@bulk_import) do |f| %>
    <% if @bulk_import.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@bulk_import.errors.count, "error") %> prohibited this expense from being saved:</h2>

          <ul>
            <% @bulk_import.errors.full_messages.each do |message| %>
                <li><%= message %></li>
            <% end %>
          </ul>
        </div>
    <% end %>
    <div class="field">
      <%= f.label :user_id %><br>
      <%= f.text_field :user_id %>
    </div>
    <div class="field">
      <%= f.label :user_id %>
      <%= collection_select(:bulk_import, :user_id, @users, :id, :name)%>
    </div>
    <div class="field">
      <%= f.label :given_date %><br>
      <%= f.date_select :given_date %>
    </div>
    <div class="actions">
      <%= f.submit %>
    </div>
<% end %>