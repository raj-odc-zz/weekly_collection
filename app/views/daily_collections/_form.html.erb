<%= form_for(@daily_collection, html: {class: 'form col-md-6 col-md-offset-3'}) do |f| %>
  <% if @daily_collection.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daily_collection.errors.count, "error") %> prohibited this daily_collection from being saved:</h2>

      <ul>
      <% @daily_collection.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :loan_id %><br>
    <%= f.text_field :loan_id %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.text_field :amount %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.text_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit  :class => "btn btn-info" %>
  </div>
<% end %>
